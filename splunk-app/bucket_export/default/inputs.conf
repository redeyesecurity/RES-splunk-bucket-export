# Scripted input — runs the bucket exporter on a schedule
# interval = 3600 means once per hour; set to -1 for one-shot on restart
[script://$SPLUNK_HOME/bin/scripts/bucket_export.sh]
interval = 3600
sourcetype = bucket_export:internal
index = _internal
disabled = 1
